"scripts": {
  "dev": "tsx server.ts",
  "build": "vite build && esbuild server.ts --bundle --platform=node --format=cjs --packages=external --sourcemap --outfile=dist/server.cjs",
  "build:client": "vite build",
  "start": "node dist/server.cjs",
  ...
}
```[span_9](start_span)[span_9](end_span)

---

### 🚀 ไฟล์ `.github/workflows/pages.yml` สำหรับ `zyrquen-frozen-v1.2-lts`

ปรับแต่ง GitHub Actions Workflow ให้ดึง Script `npm run build:client`[span_10](start_span)[span_10](end_span) พร้อมรองรับ React Router v7[span_11](start_span)[span_11](end_span) (จัดการปัญหา Single Page Application 404 Reload)[span_12](start_span)[span_12](end_span):

```yaml
name: GitHub Pages Deployment

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build-and-deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js v22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci

      # คอมไพล์เฉพาะส่วน Static Assets ของ Vite
      - name: Build Client Static Assets
        run: npm run build:client

      # คัดลอก index.html เป็น 404.html ป้องกันปัญหา Routing ของ react-router-dom
      - name: Setup SPA Fallback for React Router
        run: cp dist/index.html dist/404.html

      - name: Setup Pages
        uses: actions/configure-pages@v6

      - name: Upload Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './dist'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
